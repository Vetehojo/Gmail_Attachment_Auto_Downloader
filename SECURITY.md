# セキュリティに関する報告

## 脆弱性を見つけた場合

このリポジトリの **Security → Report a vulnerability**（GitHub Security Advisory）から
非公開で報告してほしい。公開 Issue には書かないこと。

個人で保守しているため、修正や返答の期限は約束できない。報告内容によっては
修正せず、README に既知の制約として記載する対応になる場合がある。

## このソフトウェアが扱う機微情報

- **OAuth クライアント JSON / サービスアカウント JSON / `token.json`**
  設定画面で「保存」を押したときに `%LOCALAPPDATA%\GmailAutoDownloader\` へ保存し、
  ACL の継承を切って実行ユーザーのみアクセス可能にする。これらは同一 PC の
  管理者権限を持つ者からは読み取れる。共用 PC での運用は想定していない。
- **ダウンロードした添付ファイル**
  設定した保存先にそのまま置かれる。暗号化はしない。保存先のアクセス権は
  利用者が設定すること。
- **処理キュー（`state/` の SQLite）**
  メールの件名・送信元・添付ファイル名・メール ID を保持する。本文と添付の中身は
  保持しない。成功記録は 730 日で削除し、90 日を過ぎたものは詳細を圧縮する。
  管理情報として、アカウントごとに確認したメールボックスのアドレス（接続テストか
  最初の接続で記録）と、
  認証の問題を知らせるメッセージ（アカウントのアドレスを含む）も保存する。
- **ログ（`log\`）と保存先の通知ファイル**
  `log\` のログと、保存先に書く `!取得エラー_YYYYMMDD.txt` には、メールの件名・
  添付ファイル名・メールアドレス・保存先のファイルのパス（ファイル名の形式によっては
  送信元のアドレスを含む）が残ることがある。保存先の `!監視停止_YYYYMMDD.txt` には
  監視が止まった理由を書く。通知ファイルは保存先にアクセスできる人なら読める。
  ログや通知ファイルを人に渡すときは内容を確認すること。

## アクセス権を取り消す方法

`token.json` は、失効させるまで有効な読み取り権限そのものである。
次の場合は必ず取り消すこと。

- PCを譲渡・廃棄・修理に出すとき
- PCを紛失・盗難したとき
- 利用していた担当者が退職・異動したとき

**取り消し方**: Googleアカウント → セキュリティ → サードパーティ製アプリとの連携 →
対象のアプリ → アクセス権を削除。取り消すと、そのPCの `token.json` は無効になる。

Google Workspace でドメイン全体の委任を使っている場合は、管理コンソールの
「セキュリティ → APIの制御 → ドメイン全体の委任」から、当該サービスアカウントの
委任設定そのものを削除する。これは対象ドメイン全体に一括で効く。

## 権限

要求するスコープは `https://www.googleapis.com/auth/gmail.readonly` のみ。
メールの送信・削除・既読化・ラベル変更はできない。

サービスアカウント鍵によるドメイン全体の委任を使う構成では、**その鍵で
ドメイン内の全メールボックスが読める**。アプリ側の対象アカウント設定は
利便性のための絞り込みであって、鍵そのものの権限を制限するものではない。
